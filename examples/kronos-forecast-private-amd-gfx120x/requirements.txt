# --- The SDK (this example's own runtime deps) -----------------------------
# main.py calls the workload over HTTP, so it needs the SDK + requests.
#
# local, unpublished build — see ../CONTRIBUTING.md#4-depending-on-the-sdk:
#   ( cd ../../bindings/python && maturin develop --release )
# or, once published:
#   inferencekey>=0.1.0
requests>=2.28

# --- The custom backend's deps (installed into the backend's venv) ---------
# The SDK runtime that serves the backend imports NONE of these — they belong
# to backend.py (kronos). The worker installs them into the backend's venv from
# this file (name = "kronos", requirements = "requirements.txt").
#
# NOTE: Kronos itself is NOT pip-installable (no setup.py/pyproject upstream), so
# its code is *vendored* in kronos_model/ (MIT — see kronos_model/LICENSE). There
# is no `kronos` line here; only the libraries kronos_model/ imports.

# The InferenceKey SDK runtime provides `inferencekey.backend.serve`, which the
# worker launches inside the backend venv; keep it resolvable from site-packages.
inferencekey==0.260709.1212
# Kronos's own requirements (kronos_model/kronos.py + module.py import these).
einops==0.8.1
huggingface_hub==0.33.1
safetensors
tqdm
# forecast() builds OHLCV DataFrames and stacks sample paths with numpy/pandas.
numpy<2
pandas
# Standard torch — Kronos uses no custom CUDA kernels, so a recent wheel runs on
# NVIDIA CUDA or AMD ROCm. On an AMD RDNA4 (gfx120x) node the worker points pip
# at the ROCm wheel index (torch>=2.7); on NVIDIA it comes from PyPI (CUDA).
# pick_device() then selects the accelerator, falling back to CPU.
torch>=2.2
